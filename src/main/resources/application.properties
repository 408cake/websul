spring.application.name=bookstore

spring.datasource.url=jdbc:mysql://bookstoredb.mysql.database.azure.com:3306/test
spring.datasource.username=bookstoreadmin
spring.datasource.password=Admin123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=8080
jwt.secret=dev-secret-key-dev-secret-key-dev-secret-key

jwt.access-ttl-seconds=${JWT_ACCESS_TTL_SECONDS:900}
jwt.refresh-ttl-seconds=${JWT_REFRESH_TTL_SECONDS:1209600}

spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

firebase.credentials-base64=${FIREBASE_CREDENTIALS_BASE64:}
firebase.credentials-path=${FIREBASE_CREDENTIALS_PATH:}